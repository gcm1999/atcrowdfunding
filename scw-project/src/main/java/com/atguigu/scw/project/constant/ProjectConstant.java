package com.atguigu.scw.project.constant;

public class ProjectConstant {
	
	public static final String TEMP_PROJECT_PREFIX = "project:create:temp:"; //拼接在projectToken前。用于redis中的key
	public static final String PROJECT_INFO_PREFIX = "project:info:temp:"; //拼接在projectToken前。用于redis中的key

}
